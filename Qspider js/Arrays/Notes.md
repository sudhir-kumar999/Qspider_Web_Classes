length = Array size
index Integer = number starting from zero 0
elements = value written inside an array
array object object = variable
square bracket 
empty array

way to create array

when we use array.reverse() it change the original array
different between slice and splice for mock questions
splice:- insertion deletion updation