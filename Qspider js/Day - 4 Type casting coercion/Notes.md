There are two type of type conversion 

1. Implicit type conversion  :- to convert one type of data into another type by js engine automatically is called implicit type conversion

2. Explicit type conversion :- to convert one type of data into another type forcefully by js engine is called explicit type conversion

 typeof operator is js is used to check the type of value

 Scope:- scope defines the visibility and accessibility of a code
 There are two types of scope
  1. Global Scope  :- 
  2. Local scope :-
        types of local scope
           1. Block scope
           2. function scope
           3. Script scope

js engine make a scope and make global scope at bottom part 
var a variable is stored in global scope
 let and const declares in script scope
